#include <iostream>

using namespace std;

int main() {
  string line;
  for (int i = 1; i <= 100; i++) {
    line += to_string(i) + ": ";
    
    if (i % 3 == 0) {
      line += "fizz";
    } 
    if (i % 5 == 0) {
      line += "buzz";
    }

    cout << line << "\n";

    line = "";
  }
}